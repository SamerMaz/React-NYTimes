import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
    card: {
        maxWidth:350,
        boxShadow: "0 10px 8px 0 rgba(0, 0, 0, 0.3)",
        backgroundColor: "#fafafa",
        height: 450
    },
    media: {
        height: 200
    },
});
